<p id="notice"><%= notice %></p>
<div class="panel panel-default">
  <div class="panel-heading">
    <h2 class='text text-center'>Lista de asignaturas</h2>
  </div>
  <div class="panel-body">
    <table class= "table table-striped table-bordered">
      <thead>
        <tr>
          <th>Nombre</th>
          <th colspan="4"></th>
s        </tr>
      </thead>
      <tbody>
        <% @asignaturas.each do |asignatura| %>
          <tr>
            <td><%= asignatura.nombre %></td>
            <td>
              <div class="btn-group">
                <% if can? :destroy, @asignatura %>
                  <%= link_to 'Eliminar', asignatura, method: :delete, data: { confirm: '¿Estás seguro?' },class: 'btn btn-default' %>
                <% end %>
              </div>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <% if can? :create, @asignatura %>
    <%= link_to 'Crear asignatura', new_asignatura_path, class: 'btn btn-default' %>
  <% end %>
</div>